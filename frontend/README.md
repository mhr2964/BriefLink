# Frontend Foundation

Minimal runnable frontend foundation for BriefLink.

## Assumptions
- A backend may be available at the same origin and expose `GET /api/v1/status`
- If the backend is on a different origin during local development, set `VITE_API_BASE_URL`

## Local run