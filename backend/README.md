# BriefLink Backend

Minimal runnable Express backend foundation.

## Included

- `GET /api/v1/status`
- Versioned API base under `/api/v1`
- JSON success and error envelopes with top-level `success`
- JSON `404` and `500` responses
- `brief-schema.json` describing the response envelope shape

## Response Examples

Success:
