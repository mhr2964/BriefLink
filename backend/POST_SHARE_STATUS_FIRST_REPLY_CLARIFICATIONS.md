# Post-Share Status + First-Reply Clarifications
This additive update narrows the repo-agnostic contract to the blocker-relevant items frontend needs locked now: state model, first-reply payload/response shape, validation failures, and success/error transition behavior.

## 1) Locked state model

### Canonical status enum