# Frontend Integration Handoff

## Base URL
Assume the backend is served from:

`http://localhost:3000`

All API routes are versioned under:

`/api/v1`

## Endpoints

### GET `/api/v1/status`
Health check endpoint.

#### Success response