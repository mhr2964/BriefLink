# BriefLink shared local environment baseline
# Copy relevant values into per-service .env files if the service expects them.

NODE_ENV=development

# Local service ports
PORT=4000
BACKEND_PORT=4000
FRONTEND_PORT=5173

# Frontend -> backend connectivity
API_BASE_URL=http://localhost:4000
VITE_API_BASE_URL=http://localhost:4000