name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI baseline placeholder
        run: |
          echo "BriefLink CI baseline is configured."
          echo "Add install, lint, test, and build steps when application code is present."